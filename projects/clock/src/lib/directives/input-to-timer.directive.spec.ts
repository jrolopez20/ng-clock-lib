import {InputToTimerDirective} from './input-to-timer.directive';

describe('InputToTimerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputToTimerDirective();
    expect(directive).toBeTruthy();
  });
});
